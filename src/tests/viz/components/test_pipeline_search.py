"""

"""
import pytest

class TestPipelineSearch:
    @pytest.mark.asyncio
    async def test_runner(self, mock_pipeline_batch):
        """
        """
        ## wait for task to finish
        assert mock_pipeline_batch.status in ["SUCCESS", "PENDING", "STARTED"]